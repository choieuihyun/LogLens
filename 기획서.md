# 구조화 로깅 시스템 — 설계 문서 (초안)

> 안드로이드(및 범용) 앱을 위한 **도메인 기반 구조화 로깅 표준 + 전용 실시간 뷰어/분석기.**
> 이 문서는 새 독립 repo에서 clean-room 으로 구현하기 위한 설계 초안이다. 특정 회사 코드/내부 구조와 무관한, 아이디어와 설계 결정만 담는다.

---

## 1. 문제 정의

대부분의 앱 로그는 **잡다하다**:
- 개발자마다 제각각 태그/포맷 (`Log.d("TAG", "@@@@ ...")`)
- 파싱 불가한 자유 문자열 → 기계 분석 불가
- 잡음(디버깅 마커)과 위험(토큰/PII 덤프)이 섞임

핵심 통찰: **뷰어·분석은 찍힌 로그만큼만 좋아진다 (garbage in, garbage out).**
아무리 좋은 로그 뷰어도 잡다한 로그 위에선 잡다하게 보인다. 그래서 진짜 레버리지는 **로그를 규격대로 찍게 만드는 것**이고, 뷰어는 그 규격을 착취하는 소비 도구다.

## 2. 목표 / 비목표

**목표**
- 앱이 **도메인/이벤트 규격**에 맞춰 구조화 로그를 찍게 하는 가벼운 라이브러리
- 그 규격을 이해해 **실시간 렌더 + 이슈 자동수집 + 분석 대시보드**를 주는 뷰어
- **어느 프로젝트에나** 붙는 모듈 (도메인은 프로젝트가 공급)

**비목표**
- 범용 logcat 뷰어로 Android Studio Logcat / 상용 도구를 이기는 것 (X). 차별점은 **"구조화된 도메인 로그의 분석"** 뿐이다.
- 서버 로그 수집 파이프라인(ELK 등) 대체 (X). 온디바이스/개발단계 초점.

## 3. 아키텍처 개요

두 컴포넌트, **오직 "로그 레코드 포맷(계약)"으로만 연결**된다.

```
[앱] --구조화 로그--> [기기 logcat 버퍼]
   (A) 로깅 라이브러리          │ adb
                                ▼
                      [ (B) 뷰어/분석기 ]
```

- 두 컴포넌트는 코드 의존이 없다. 계약(포맷)만 공유 → 각자 독립 배포/발전.
- 라이브러리를 안 써도 뷰어는 일반 로그를 보여주고(색만), 라이브러리 로그는 구조화해서 리치하게 → **call site #1 부터 점진 도입 가능.**

## 4. 레코드 규격 (계약)

logcat 과 파일 싱크가 **동일한 한 줄** 규격을 쓴다. 파서 하나로 두 소스 처리.

```
<레벨>/APP_<도메인>: evt=<이벤트> key=value key=value | <자유 메시지>
```
예:
```
I/APP_AUTH: evt=LOGIN_OK uid=123 | 로그인 성공
E/APP_NET:  evt=SOCKET_FAIL host=10.0.0.1 err=ConnectException:timeout
```

**불변식(invariants)**
- **태그 = `<prefix>_<도메인>`** → 도메인이 곧 logcat 태그. `adb logcat -s APP_AUTH:*` 로 도메인 필터. (구식 래퍼들이 "태그를 메시지 본문에 넣어" 태그 필터를 무의미하게 만든 실패를 피함)
- **한 줄 = 한 레코드** → logcat 4KB 근처 잘림 대비, 초과분은 통째로 자르고 `...[cut]` (여러 줄 분할 금지 — 파서가 깨짐)
- **evt** 이벤트 코드, 영문 대문자 SNAKE_CASE
- **key=value** 값에 공백/구분자 없음 (emitter 가 sanitize)
- **`|` 뒤** 자유 메시지(공백 허용)

파서 정규식:
```
^(?:(?<time>\d{4}-\d\d-\d\d \d\d:\d\d:\d\d\.\d{3}) )?(?<level>[VDIWE])/<PREFIX>_(?<domain>[A-Z]+)(?:\(\s*\d+\))?: evt=(?<event>\S+)(?<fields>(?: \w+=\S+)*)(?: \| (?<msg>.*))?$
```

## 5. 컴포넌트 A — 로깅 라이브러리 (emitter)

### API (파사드)
```
Log.i(domain, "EVENT", "k1", v1, "k2", v2)      // varargs 키-값 쌍
Log.e(domain, "EVENT", throwable, "k", v)         // 예외 오버로드
Log.d(domain, "EVENT")                            // 이벤트만
// "msg" 키 → 자유 메시지로 " | " 뒤에 렌더
```
- **왜 varargs 쌍인가**: `Log.d(TAG, "..")` 대비 마찰이 크지 않아야 채택된다. 빌더 패턴은 장황해서 기각.
- 트레이드오프: 컴파일 타임 키-값 짝 안전성 없음 → **DEBUG 빌드에서 홀수 인자 경고 가드**로 보완.

### 모듈화 핵심 — Domain 을 앱이 공급
도메인 목록은 **프로젝트의 정체성**이다(앱마다 다름). 라이브러리에 박으면 재사용 불가.
```
// 라이브러리: 도메인을 모른다
interface LogDomain { String tag(); }

// 앱: 자기 도메인 정의
enum AppDomain implements LogDomain {
    AUTH, CHAT, FILE, NET, ...;
    public String tag() { return "APP_" + name(); }
}
```
→ 라이브러리는 도메인 불가지(agnostic), 각 프로젝트가 자기 도메인을 꽂는다. **이게 "any project" 의 핵심.**

### PII 마스킹 (emitter 단)
- 민감 키(password/token/secret/phone/email/ssn 등)는 **키 이름 기반으로 emitter 가 값 마스킹.** 호출부는 신경 안 씀 → 기존 call site 를 안 건드리고 정책 적용.
- **주의(실전 교훈)**: 부분 일치는 과잉 마스킹을 부른다(`auth` 가 `authType` 까지 가림). **정확 키 매칭 또는 어간 신중 선정.**

### 릴리스 게이팅
- `V`/`D`: DEBUG 빌드만. `I`/`W`/`E`: 항상.
- 모듈화 시 함정: **라이브러리의 `BuildConfig.DEBUG` 는 앱 빌드타입을 모른다.** → `Log.init(isDebug)` 로 앱이 주입하거나 `ApplicationInfo.FLAG_DEBUGGABLE` 런타임 확인.

### 싱크 (출력 대상) — 플러그블
- 기본: logcat. 선택: 파일(날짜별), 향후: 크래시 리포터 브레드크럼.
- 모듈화 시 파일 싱크를 라이브러리에 내장(호스트 유틸 의존 제거)하거나 `Sink` 인터페이스로 주입.

## 6. 컴포넌트 B — 뷰어 / 분석기

### 아키텍처의 핵심 트릭 — 로컬 서버 + SSE
브라우저는 **USB/adb 에 직접 접근 못 한다.** 그래서:
```
[기기] --adb logcat--> [로컬 서버(파싱)] --SSE--> [브라우저 GUI]
```
- 로컬 프로세스가 `adb logcat` 을 물어 파싱 → **SSE(Server-Sent Events)** 로 브라우저에 푸시.
- 결과: **adb 실시간 스트리밍 + 웹 GUI** 를 Electron 없이, 파일 주입 없이. (면접에서 "브라우저 샌드박스 한계를 로컬 브리지로 우회" 설명 포인트)
- 복원력: logcat 스트림이 끊겨도(기기 sleep/재연결) **자동 재연결.**

### 기능
- **도메인 탭** — 로그인/채팅/... 원클릭 필터. (이관 전 레거시도 태그 정규식으로 함께 잡아 점진 도입 매끄럽게)
- **레벨/검색/정규식 필터**, 대상 앱 pid 자동추적
- **이슈 트레이** — 크래시(예외 클래스)/ANR/네이티브/구조화 에러를 **자동 그룹핑 + 카운트.** 클릭 시 그 이슈만 필터. (범용 로그캣엔 없는 기능)
- **분석 대시보드** — 버퍼를 group-by 집계: **성공률, 실패 사유 분포, 단계별 이탈(퍼널), 도메인/이벤트 빈도.** ← 구조화 로그라서 가능한, 범용 도구가 원리상 못 하는 것.

### 모듈화 — config 외부화
- 지금은 탭/이슈규칙이 코드에 하드코딩. 재사용하려면 **`config.json` 으로 분리**(도메인 탭, 이슈 규칙, 프리픽스) → 프로젝트별 갈아끼움.
- 배포: pip CLI 또는 PyInstaller 단일 실행파일.

## 7. 모듈화 / 배포 형태

| 컴포넌트 | 형태 | 사용 |
|---|---|---|
| 라이브러리 | `.aar` / Maven(사내 Nexus·JitPack) | `implementation 'x.y:log:1.0'` + 앱이 도메인 정의 |
| 뷰어 | pip CLI / 단일 바이너리 + config.json | `logcat-view --config app.json` |
| 데모앱 | 샘플 도메인 + 미니 앱 | 비공개 앱 없이도 데모 실행 |

## 8. 설계 결정 & 근거 (핵심 — 이력서/면접용)

| 결정 | 근거 |
|---|---|
| 도메인 = logcat 태그 | 태그 레벨 필터가 그대로 도메인 필터. 구식 래퍼(태그를 본문에 넣음)의 필터-무력화 실패 회피 |
| 한 줄 = 한 레코드 (분할 금지) | logcat 4KB 잘림에도 파서 불변식 유지 |
| Domain 을 앱이 공급(인터페이스) | 라이브러리 도메인 불가지 → 진짜 재사용 |
| PII 마스킹을 emitter 단에 | 수천 call site 안 건드리고 정책 일괄 적용 |
| 뷰어를 로컬서버+SSE 로 | 브라우저 USB 제약 우회, Electron 불필요 |
| 구조화 강제(파싱가능 규격) | "분석 도구에 들어가려면 규격을 지켜야" 하는 보상 → 채택 유도 |
| 이관은 추가가 아니라 "교체" | 추가만 하면 잡음+구조로 2배 (실전 함정) |

## 9. 실전 교훈 (lessons learned)

1. **추가형 이관의 함정** — 새 구조 로그를 넣기만 하고 옛 로그를 안 지우면 오히려 잡음이 늘어난다. 반드시 옛것 제거/전환.
2. **마스킹 과잉** — 민감 키 부분 일치가 멀쩡한 필드까지 가린다. 정확 매칭.
3. **절대 로그 금지** — 토큰/JWT/비밀번호/응답 JSON·XML 통째/개인정보 원문. (실전에서 인증 응답 통째 덤프로 유효 토큰이 로그에 노출된 사례를 겪음 → 보안 사고)
4. **이름 거버넌스** — 이벤트/필드 이름이 자유 문자열이면 드리프트(`LOGIN_END` vs `LoginEnd`, `uid` vs `userId`)해서 분석이 쪼개진다. 규칙 + 공통 필드 표준 + 가능하면 상수화.
5. **상관 ID 는 초기에 결정** — 세션 퍼널 분석엔 flowId 가 필요. 나중에 넣으면 call site 전부 수정.

## 10. 로드맵

- 뷰어: 세션 저장/공유(`.session` 파일), 타임라인/미니맵, retrace(난독 스택 복원), 이벤트 상수 자동수집
- 라이브러리: Kotlin 우선(코루틴/DSL API 고려), 이벤트 코드 상수화 도구, 멀티 싱크
- 분석: 시간대별 추이, 세션 단위 퍼널, 이상 탐지

## 11. 포트폴리오 하이라이트 (강조 포인트)

- "잡다한 로그 → 규격화 → 분석 가능" 이라는 **문제 정의와 레버리지 판단** (뷰어가 아니라 emission 이 병목이라는 통찰)
- **decouple 설계** — 도메인 인터페이스로 라이브러리를 프로젝트 불가지로
- **브라우저 샌드박스 우회** — 로컬 서버 + SSE 브리지
- **파싱 불변식** — 한 줄=한 레코드, 4KB 잘림 처리
- **보안 의식** — PII 마스킹, 토큰 로깅 방지 (실사고 경험 기반)
- **점진 채택 전략** — call site #1 부터 가치, 레거시 공존

## 12. clean-room 주의

- 새 repo 는 회사 코드/히스토리와 **완전 분리**. 개인 GitHub.
- 회사 도메인 목록·서버 프로토콜·실데이터·내부 명칭 **전부 제외**. 예시는 일반적인 것(AUTH/CHAT/FILE 등 흔한 앱 도메인)으로.
- 코드 복붙 대신 **범용으로 재구현**. 설계·아이디어는 본인 것이나, 산출물 코드의 소유권 회색지대를 피함. 불명확하면 근로계약 IP 조항 확인.

---

## 부록 — 제안 repo 구조

```
<name>/                     (중립 이름: StructLog / LogLens / Tagcat 등)
├─ lib/                     로깅 라이브러리 (Kotlin 권장)
│   ├─ core (Logger, LogDomain 인터페이스, Sink)
│   └─ sample-domains
├─ viewer/                  뷰어 (로컬서버 + web UI + config)
│   ├─ server (adb 스트리밍 → SSE)
│   ├─ web (탭/이슈트레이/대시보드)
│   └─ config.schema.json
├─ sample-app/              데모용 미니 앱
├─ docs/                    아키텍처 다이어그램, 설계결정
└─ README.md                문제정의 + GIF + 빠른시작
```
